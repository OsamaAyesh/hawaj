import 'package:flutter/material.dart';
class RowWithDollarIconPriceWidget extends StatelessWidget {
  const RowWithDollarIconPriceWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        // dollarIcon
      ],
    );
  }
}
